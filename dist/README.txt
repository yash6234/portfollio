Place your personal photo as `profile.jpg` in this folder.
Vite serves files in `public/` from the site root, so the app
will load it from `/profile.jpg`. If the file is missing, it
falls back to a bundled SVG silhouette.

